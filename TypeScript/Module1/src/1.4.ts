{
    // Basic data type of TypeScript 
    // number 
    const age: number = 4
    // string 
    const name: string = "Toukir"
    //boolean
    const isTure: boolean = true
    //null
    const x: null = null
    // undefined
    const y: undefined = undefined

    // array 
    const names: string[] = ["Toukir", "Tanvir"]
    const ages: number[] = [10, 12]

    // tuple => is an special type of array where you can make order type of array
    const agesCordinate: [number, number] = [5, 5]
    const ageName: [number, string] = [10, "toukir"]
}